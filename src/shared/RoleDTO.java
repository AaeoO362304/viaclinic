package shared;

/**
 * The user roles (patient, doctor, receptionist) sent between the server and the client.
 *
 * @author Kühn, Pástor, Kolodziejczyk, Bastola, Karki
 * @version 1.0
 */
public enum RoleDTO {
    /** Role for patient users. */
    PATIENT,
    /** Role for doctor users. */
    DOCTOR,
    /** Role for receptionist users. */
    RECEPTIONIST
}
