package server.model.auth;

/**
 * The user roles in the system: patient, doctor and receptionist.
 *
 * @author Kühn, Pástor, Kolodziejczyk, Bastola, Karki
 * @version 1.0
 */
public enum Role
{
    /** Role for patient users. */
    PATIENT,
    /** Role for doctor users. */
    DOCTOR,
    /** Role for receptionist users. */
    RECEPTIONIST
}
